#pragma once

#include <wordring/whatwg/url/host.hpp>

#include <optional>
#include <string>
#include <vector>

namespace wordring::whatwg
{
	/*
	class url_record
	{
	public:
		using string_type = std::u32string;
		using host_type = wordring::whatwg::host;
		using port_type = std::uint16_t;
		using path_type = std::vector<std::string>;
		using blob_type = std::vector<std::uint8_t>;

	protected:
		std::string                m_scheme;
		std::string                m_username;
		std::string                m_password;
		std::optional<host_type>   m_host;
		std::optional<port_type>   m_port;
		path_type                  m_path;
		std::optional<std::string> m_query;
		std::optional<std::string> m_fragment;

		bool      cannot_be_base_url_flag = false;
		blob_type blob_url_entry;
	};
	*/
}
